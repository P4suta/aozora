# Build / packaging artefacts — regenerated by `bun run build` and the
# release workflow; never committed.
node_modules/
out/
*.vsix
# Staging dir for the bundled aozora-lsp binary (release-vscode.yml).
server/
