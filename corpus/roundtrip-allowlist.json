{
  "note": "Round-trip (parse∘serialize fixed-point) divergences. EMPTY: the normalization-waist lowering pass (lower_spans overlap-truncate) dissolved the #180 reclaimed-forward-strand class, so every corpus document is a fixed point by construction. The surviving ForwardOrigin (Reclaimed/Referenced) provenance is necessary, not a divergence (#202). SHRINK-ONLY: never add entries; fix the serializer/lowering pass instead.",
  "allow": []
}
